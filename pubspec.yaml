name: inventigacionflutter
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  english_words: ^4.0.0
  provider: ^6.0.0
  dio: ^5.8.0+1
  hive_flutter: ^1.1.0
  either_dart: ^1.0.0
  bloc: ^9.0.0
  flutter_bloc: ^9.1.1

dev_dependencies:
  mocktail: ^1.0.0
  flutter_test:
    sdk: flutter

  mockito: ^5.4.2
  build_runner: ^2.4.6
  provider: ^6.0.0

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

